{
    "name": "@nonnajs/compiler",
    "version": "1.0.0",
    "description": "Ahead-of-Time TypeScript TypeChecker-based dependency compiler for @nonnajs/di. Infers constructor dependencies at build time with zero runtime reflection.",
    "author": "Manuel Santos <ney.br.santos@gmail.com>",
    "license": "MIT",
    "keywords": [
        "di",
        "dependency-injection",
        "aot",
        "compiler",
        "nonna"
    ],
    "repository": {
        "type": "git",
        "url": "https://github.com/nonnajs/nonna.git"
    },
    "publishConfig": {
        "access": "public"
    },
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "bin": {
        "nonna-compile": "./dist/cli.js"
    },
    "scripts": {
        "build": "tsc -p tsconfig.build.json",
        "clean:build": "rimraf ./dist",
        "lint": "eslint .",
        "lint:fix": "eslint . --fix",
        "format": "prettier --ignore-path ../.prettierignore --check .",
        "format:fix": "prettier --ignore-path ../.prettierignore --write .",
        "test": "node --test --test-reporter node-test-reporter --require ts-node/register test/**/*.{test,it.test}.ts",
        "pretest:coverage": "mkdir -p coverage",
        "test:coverage": "node --experimental-test-coverage --test-coverage-include='src/**/*.ts' --test-coverage-exclude='src/**/*.test.ts' --test-coverage-exclude='src/**/*.spec.ts' --test --test-reporter node-test-reporter --test-reporter-destination stdout --test-reporter lcov --test-reporter-destination coverage/lcov.info --test-reporter junit --test-reporter-destination coverage/junit.xml --require ts-node/register test/**/*.{test,it.test}.ts",
        "tsc": "tsc"
    },
    "peerDependencies": {
        "typescript": ">=5.0.0"
    },
    "devDependencies": {
        "typescript": "^5.9.2",
        "@nonnajs/di": "workspace:*"
    },
    "files": [
        "dist",
        "package.json",
        "README.md",
        "LICENSE"
    ]
}
