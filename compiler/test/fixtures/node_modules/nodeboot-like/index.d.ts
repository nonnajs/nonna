// Stands in for a framework built on top of @nonna/di (e.g. Node-Boot's own
// @Component/@Inject), installed as a real package under node_modules, so
// `createInjectorModuleMatcher` can resolve it exactly like the real @nodeboot/core would be.
export declare function Component(
    options?: unknown,
): (target: Function) => void;

export declare function Inject(
    token?: unknown,
): (target: object, propertyKey: string | symbol | undefined, parameterIndex: number) => void;
